{{ form_start(form) }}
    <div class="form-group">
        {{ form_label(form.nbSeats, 'Number of Seats') }}
        {{ form_errors(form.nbSeats) }}
        {{ form_widget(form.nbSeats, {'attr': {'class': 'form-control'}}) }}
    </div>

    <div class="form-group">
        {{ form_label(form.nbDoors, 'Number of Doors') }}
        {{ form_errors(form.nbDoors) }}
        {{ form_widget(form.nbDoors, {'attr': {'class': 'form-control'}}) }}
    </div>

    <div class="form-group">
        {{ form_label(form.name, 'Car Name') }}
        {{ form_errors(form.name) }}
        {{ form_widget(form.name, {'attr': {'class': 'form-control'}}) }}
    </div>

    <div class="form-group">
        {{ form_label(form.cost, 'Car Cost') }}
        {{ form_errors(form.cost) }}
        {{ form_widget(form.cost, {'attr': {'class': 'form-control'}}) }}
    </div>

    <div class="form-group">
        {{ form_label(form.category, 'Category') }}
        {{ form_errors(form.category) }}
        {{ form_widget(form.category, {'attr': {'class': 'form-control'}}) }}
    </div>
    <div class="d-flex justify-content-center">
        <button class="btn btn-success btnSpace">{{ button_label|default('Save') }}</button>
    </div>
{{ form_end(form) }}